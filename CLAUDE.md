
# General instructions
- Never write "Claude" or "AI" or anything like that on a git message or code comment.
- The app should work on docker compose. Please run all app commands using docker compose exec ... so that the code works on the right environment and we ensure it works everywhere.
- When planning ahead, always write the plan to SCRATCHPAD.md which is ignored from version control.
- When working on a new assingment, create a branch and a PR. On every commit, update the PR description with the contents of SCRATCHPAD.md.
- Read the /docs directory.
- Use Google cloud Plataform if need cloud service


